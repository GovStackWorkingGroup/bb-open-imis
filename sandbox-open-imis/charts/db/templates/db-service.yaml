apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.name }}
spec:
  ports:
    - port: {{ .Values.dbService.port }}
  selector:
    app: {{ .Values.global.name }}
  clusterIP: None